node_modules/
.next/
out/
build/
next-env.d.ts
src/generated/

*.tsbuildinfo

.env
.env*.local

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.vercel

*storybook.log
storybook-static

e2e/.auth
playwright-report
test-results
blob-report
.playwright-mcp

.agents/
.codex/
.mcp.json

.scratch/

# Reset snapshots and sign-off sheets — a full stock position for a real
# trading day, produced by scripts/reset-snapshot.ts. Kept out of the repo
# deliberately: the artifacts belong with the operator running the reset,
# alongside the pg_dump, not in version control.
scripts/reset/
