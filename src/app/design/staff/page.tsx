import { Suspense } from "react";
import { StaffRound } from "@/components/design/staff/round";

export default function StaffPage() {
  return (
    <Suspense>
      <StaffRound />
    </Suspense>
  );
}
