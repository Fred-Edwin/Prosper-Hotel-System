import { Suspense } from "react";
import { FrameRound } from "@/components/design/shell/frame-round";

export default function FramePage() {
  return (
    <Suspense>
      <FrameRound />
    </Suspense>
  );
}
